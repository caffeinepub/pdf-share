{
  "frontend/src/index.css": "Global styles with dark navy + warm amber OKLCH color system, Google Fonts (Inter + Space Grotesk), and utility component classes for cards, buttons, and nav links.",
  "frontend/tailwind.config.js": "Extended Tailwind config with custom font families (Inter/Space Grotesk), amber/navy color tokens, card shadow, glow shadow, and fade-in animation.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — includes existing upload/delete/rename mutations and list/get queries, plus three new admin hooks: `useBannedUsers` (query for listing banned principals), `useKickUser` (mutation to ban a user), and `useUnbanUser` (mutation to remove a ban), all invalidating the `bannedUsers` query key on success.",
  "frontend/src/components/Header.tsx": "Sticky navigation header with a new \"Feed\" link (Rss icon) added between Gallery and Upload, using the same active-route highlighting pattern as the other nav links.",
  "frontend/src/components/Footer.tsx": "Footer with copyright year, caffeine.ai attribution link with UTM params, and a heart icon in amber.",
  "frontend/src/pages/HomePage.tsx": "Landing page with hero section, how-it-works feature cards, and trust indicators linking to upload and dashboard.",
  "frontend/src/pages/UploadPage.tsx": "PDF upload page with chunked upload flow (startUpload → uploadChunk → finalizeUpload), actor-readiness guards, retry connection mechanism, and updated error messaging — authorization errors now clearly state the user must be logged in (removing the old \"log out and log in again\" instruction), reflecting the open upload policy for all authenticated users.",
  "frontend/src/pages/DashboardPage.tsx": "Admin dashboard with PDF list management (view, copy-link, delete, inline title editing) plus an Admin-only User Management panel — visible only when `isCallerAdmin()` returns true — featuring a kick-by-principal form with confirmation dialog and a banned users list with per-entry Unban buttons, loading spinners, and error feedback.",
  "frontend/src/pages/ViewerPage.tsx": "Public PDF viewer that reads the `shareId` route param, waits for the actor to be ready before fetching metadata via `useGetPdf`, then streams the PDF via XHR with progress tracking; properly distinguishes actor-initializing / meta-loading / PDF-fetching loading states and shows user-friendly error messages on failure.",
  "frontend/src/App.tsx": "Main app with TanStack Router — adds a new public `/feed` route rendering `FeedPage` alongside the existing routes (`/`, `/gallery` redirect, `/upload`, `/dashboard`, `/view/:shareId`).",
  "frontend/src/pages/GalleryPage.tsx": "Public gallery page that fetches all PDFs via `useListPdfs` and renders them in a responsive card grid; each card uses `useNavigate` with an explicit onClick handler (replacing the Link wrapper) to reliably navigate to `/view/:shareId`, with keyboard accessibility support.",
  "frontend/src/pages/FeedPage.tsx": "New public community feed page that fetches all PDFs via `useListPdfs`, sorts them newest-first by `uploadedAt`, and renders them in a responsive card grid with title, filename, date/time, and an Open button navigating to `/view/:shareId`."
}